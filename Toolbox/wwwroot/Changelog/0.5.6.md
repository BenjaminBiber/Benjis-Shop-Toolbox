# 0.5.6
## Hinzugefuegt
- Neue Inhalte in `Einstellungen.md` hinzugefügt.
- Erweiterte Funktionen in `Toolbox.Data/Models/ToolboxSettings.cs`.
- Verbesserte Benutzeroberfläche in `GeneralSettings.razor`.
- Neue Optionen in `IisSettings.razor` hinzugefügt.
- Zusätzliche Einstellungen in `LogSettings.razor`.
- Hilfeseiten in `SettingsHelp.razor` erweitert.
- Neue Stile in `app.css` implementiert.
- Neue Stile in `app.scss` hinzugefügt.
- Erweiterte SCSS-Komponenten in `_settings.scss`.

## Geaendert
- Anpassungen in der Benutzeroberfläche von `GeneralSettings.razor`.
- Überarbeitete Einstellungen in `IisSettings.razor`.
- Verbesserte Logik in `LogSettings.razor`.
- Überarbeitete Struktur und Inhalte in `SettingsHelp.razor`.

## Behoben
- Keine Eintraege.